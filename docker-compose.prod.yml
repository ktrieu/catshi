services:
  api:
    build:
      target: prod
    restart: unless-stopped
    volumes:
      - ${PROD_SQLITE_DIR}:/app/db

  bot:
    build:
      target: prod
    restart: unless-stopped
    volumes:
      - ${PROD_SQLITE_DIR}:/app/db
