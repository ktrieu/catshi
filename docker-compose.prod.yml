services:
  catshi-api:
    build:
      target: prod
    restart: unless-stopped
    volumes:
      - ${PROD_SQLITE_DIR}:/app/db

  catshi-bot:
    build:
      target: prod
    restart: unless-stopped
    volumes:
      - ${PROD_SQLITE_DIR}:/app/db
